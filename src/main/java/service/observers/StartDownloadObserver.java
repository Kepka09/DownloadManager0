package service.observers;

import model.observer.DownloadObserver;
import model.observer.DownloadTask;

public class StartDownloadObserver implements DownloadObserver {
    @Override
    public void update(DownloadTask task) {
        //Todo do something if download is started
    }
}
